import { GeneralContainerInterpreter } from 'modules/containers';

export const ResContainers = [
    {
        ref: GeneralContainerInterpreter,
        selector: '[general]',
        name: 'General',
        shape: 'window-max',
        description: 'Container'
    }
]